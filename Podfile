# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'LanguageOM Student' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LanguageOM Student
  pod 'Firebase'
  pod 'Firebase/Database'
  pod ‘Firebase/Auth’
  
  target 'LanguageOM StudentTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LanguageOM StudentUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
